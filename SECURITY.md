# Security

Do not commit signing certificates, provisioning profiles, device identifiers,
keystores, passwords, access tokens, HAP files, or local SDK archives.

Report a suspected secret exposure through a private GitHub security advisory.
Do not include the secret value in a public issue.

