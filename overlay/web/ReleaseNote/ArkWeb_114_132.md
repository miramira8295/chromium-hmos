**注** : 此文档为已识别出的大颗粒非兼容性变更，将持续更新。

# ArkWeb 版本的差异总结

Chromium 上游社区 114 到 132 版本大颗粒变更共 **377** 项，带来了更多 W3C 新特性，以及安全，性能，稳定性提升外，识别出需要前端开发者注意及适配的，包括 W3C 规格废弃，安全限制，以及规格实现错误修复等共计 62 项。

# 114内核 -> 132 内核带来的收益总结

1. 安全能力提升：修复漏洞，更严格的跨域访问限制，防止广告等引导用户点击行为。
2. 性能提升：132 内核较 114 内核部分场景在功耗以及滑动体验上均有提升。
3. 新增 W3C 功能及特性：

   * Scroll-driven animation  更好性能的滚动驱动动画。
   * Shared storage API 存储共享。
   * Display and content-visibility animations 元素显示隐藏支持 css 动画。
   * Device Posture API – 折叠屏 API。
   * View Transitions Same-Origin Navigation 同源页面跳转动画。

# 开发者适配注意事项

### 上游社区带来的适配注意事项

OpenHarmony 的Web内核跟随系统进行升级，系统版本升级覆盖需要一定周期，导致线上将同时出现两个内核版本，Web 业务需同时兼容两个版本内核，ArkWeb 团队已识别出需要注意的场景如下，方便开发者进行适配。<br />
排查范围包括：Chrome Release Note，Chromium issue list，WPT，XTS 测试结果，头部生态应用摸测等。

### 未跟随特性
部分 Chromium 社区引入的变更，模测 TOP 应用时发现对应用主链路有影响，如下特性暂不跟随 132 内核升级，但仍然建议应用跟随Chromium最新规范进行适配。

1. input 组件如果为 disabled 状态，则不再接收 click 事件。
2. line-height 取整方式变更，会导致部分界面显示异常。
3. Input 标签的默认样式变更，导致布局错乱。
4. 自定义协议的 url 检测规范更严格，导致页面加载失败，白屏。
5. justify-self 支持父元素为 display='block' 类型的组件，之前仅支持 display='grid'，导致部分页面布局错乱。
6. Text 表示的 Emoji 文本，回落到系统字体，与之前版本不一致，影响观感。
7. 禁止设置 document.domain，导致部分业务异常，白屏。

### 开发者需特别关注的变更，需开发者跟随最新标准进行适配
1. WebKit 开头的 fullscreen API 被废弃。 | [Remove Prefixed HTMLVideoElement Fullscreen APIs](https://chromestatus.com/feature/5111638103687168)
2. 老的 WebKit Mutation Event 被废弃。 | [Deprecate Mutation Events](https://chromestatus.com/feature/5083947249172480)
3. CSS zoom 标准适配变更，导致 JS API,这改变了各种 JS API 以符合规范（参见设计文档 https://chromestatus.com/feature/5198254868529152）
4. FontFaceSet.check()对于不存在的字体返回false的逻辑发生变化
5. WebSQL 能力正式废弃

### 分版本需要关注的适配点

| 版本 | 需要适配个数 | 需要特别注意的变更                                                                                                                                                                                                           |
| ---- | ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 115  | 2            | 禁用 document.domain 设置。【未跟随】<br />跨 iframe 的数据存储与读取共享受到影响。                                                                                                                                                        |
| 116  | 3            | Mouse 以及 Pointer enter leave事件将不再合并。<br />input 标签如果为 disabled 状态，点击事件不会传递。<br>（前端网页可参考下面两种方案适配：<br>1、 将pointer-event:none 应用于受影响的 disabled 元素。<br>2、监听 pointerup/pointerdown 而不是 click 事件。）<br />wasm 全局对象默认值变更。                                                                |
| 117  | 5            | onBeforeUnload 事件的返回值处理变化。<br />ORB 更严格。<br />URL 中允许在path中存在 %00，但 host 中仍不允许。<br />v8 废弃 Array group 以及 groupToMap 语法。<br />Symbol.toStringTag 返回值类型变化。                                 |
| 118  | 4            | cookie 中包含控制字符则丢弃。<br />URL path 规格变更。<br />自动切到 http3，如果服务端支持。<br />将 XML 文档中的相邻 CDATA 节点合并为一个 CDATA 节点。                                                                              |
| 119  | 6            | 跨域的redirect，丢弃Authorization header。<br />new Url 参数 host 中包含特殊字符则抛异常。<br />为了安全，targetName 中包含特殊字符，则变为空白页面。<br />去除了对WebSQL的支持。<br />废弃非标属性shadowroot。<br />废弃Sanitizer API。 |
| 120  | 3            | 废弃 Theora 视频编码协议。<br />CSP Embedded Enforcement逻辑变更。<br />Transitions API不允许传入空参数。                                                                                                                                                                |
| 121  | 2            | 跨域 iframe 间禁止拖拽。<br />跨域 iframe 移动后用户输入事件相关。                                                                                                                                                               |
| 122  | 5            | CSS @container 包含不存在的 feature 将完全失效。<br />iframe 中如果 mousedown 被cancel，则不再发送mouseup及mouseleave事件。<br />WebRTC getStats 的 callback 方式废弃，改用 Promise 方式。<br />backdrop继承逻辑改变。<br />DataTransfer 的 clearData 行为变更，不清理 file 协议内容。               |
| 123  | 2            | 本地网络请求不会带上accept header。<br />inline script 也可以阻塞渲染，加上 blocking 标签，之前不阻塞渲染。                                                                                                                      |
| 125  | 3            | 删除亮度接近 100% 或 0 的 （ok）lab/lch 颜色的不连续性。<br />navigator.permissions.query({name: 'window-placement'})  这种用法将不支持。<br />废弃非标准的 shoadowroot getInnerHTML方法。                                                                                        |
| 126  | 3            | CSSPageRule继承于CSSGroupingRule而不是CSSRule。<br />v8 废弃 assert 语法。<br />AsyncFromSyncIteratorPrototype 行为规范化。                                                                                                        |
| 127  | 1            | 老的 WebKit Mutation Event 被废弃。                                                                                                                                                                                            |
| 128  | 2            | position-try-options 重命名为 position-try-fallbacks。<br />JS 参数个数限制为 65535，之前无限制。                                                                                                                                |
| 129  | 1            | PointerEvent.getCoalescedEvents 不允许在非安全环境使用。                                                                                                                                                                       |
| 130  | 2            | 嵌套css样式的写法，按照先后顺序，而非嵌套顺序。<br />PointerEvent 相关，JS 生成的将不做事件合并。                                                                                                                                |
| 132  | 4            | 修复Selection.isCollapsed()在 shadowDOM shu树中一致返回 true 的问题。<br />非 active document 弹出窗口/对话框将引发异常。<br />WebKit 开头的 fullscreen API 被废弃。<br />性能监控：PerformanceResourceTiming 的 initiatorType 发生变更。                                                                                                                                                                               |

## 如何根据 UA 进行内核版本判断

### 114 内核UA如下：

Mozilla/5.0 (Phone; OpenHarmony 5.0) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/114.0.0.0 Safari/537.36 ArkWeb/4.1.6.1 Mobile

### 132 内核UA如下：

Mozilla/5.0 (Phone; OpenHarmony 6.0) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/132.0.0.0 Safari/537.36 ArkWeb/4.1.6.1 Mobile

### 如何判断版本号示例

```
var arr = navigator.userAgent.split(' ');
var chromeVersion;
for(var i=0;i < arr.length;i++) {
  if(/chrome/i.test(arr[i])) {
    chromeVersion = Number(arr[i].split('/')[1].split('.')[0]);
  }
}
if (chromeVersion == 132) {
  // TODO
} else if (chromeVersion == 114) {
  // TODO
}
```

## 安全相关：

| 简要描述 | 上游社区公告 | 变更描述 | 上游Chromium变更版本 | 适配建议 | 适配后历史版本需要隔离 | 代码层面问题 | 可以静态扫描 |  示例 | 搜索方式 |
| ---- | ----------  | ---------------- | ---------------- | ------- | ---------------- | -------------- | ---------- |  ------ | ------ |
| 禁用 document.domain 的设置。【未跟随】  | [Feature: Deprecate the \`document.domain\` setter.](https://chromestatus.com/feature/5428079583297536) | 遵从 W3C 规范， document.domain 不再允许设置。<br />ArkWeb 已默认关闭该特性。 | 115 | 设置 domain 通常是为了绕过跨域访问限制（不同端口），存在安全隐患，需要业务进行适配，避免跨域页面间进行非法交互。 | 否 | 是 | 是 |  | document.domain= |
| 跨 iframe 的数据存储与读取共享受到影响。 | [Partitioning Storage, Service Workers, and Communication APIs](https://chromestatus.com/feature/5723617717387264)  | 为了解决已知的三方资源场景下的隐私和安全问题，对三方API进一步的数据隔离，使用顶层窗口网站+子窗口网站作为key，避免页面访问到不该访问的数据。 | 115 | 遵循最新的安全规范，使用 DisableThirdPartyStoragePartitioning。 | 否 | 是 | 否  | |  |
| 废弃 Theora 视频编码协议。 | [Deprecate and remove Theora support.](https://chromestatus.com/feature/5158654475239424) | 由于出现安全风险，桌面版 Chrome 浏览器将弃用并停止支持 Theora 视频编解码器。Theora 的使用率很低（而且如今其统计数据往往已不准确），继续为大多数用户提供支持已不再合理。 | 120 | 不使用该协议，换用更安全的通用协议。 | 否 | 否 | 否  | [相关示例参考](#移除了对theora和vp8的支持) |  |
| CSP Embedded Enforcement逻辑变更。 | [Feature: Remove same-origin blanket enforcement in CSPEE](https://chromestatus.com/feature/5098158594195456) | 删除same-origin blanket功能， CSP Embedded Enforcement逻辑在跨域和非跨域iframe场景一致。 | 120 | 全网使用低于 0.000017%。 | 否 | 否 | 否 | |  |
| 跨域重定向相关。 | [Remove Authorization header upon cross-origin redirect](https://chromestatus.com/feature/5195900413018112) | 跨域的redirect，丢弃Authorization header。遵守规范。 | 119 | 需要检查是否依赖跨域 Authorization header，Chrome 现网使用场景为万分之2以下。 | 否 | 否 | 否 | |  |
| PointerEvent 相关。 | [Coalesced/predicted events in untrusted PointerEvents will retain original targets](https://chromestatus.com/feature/5106728480538624) | JS 生成的 PointerEvent 将不做事件合并，避免安全隐患。 | 130 | 该用法存在安全隐患，禁止使用，需要用户点击。 | 否 | 是 | 是 | | getCoalescedEvents<br />getPredictedEvents |
| 跨域 iframe 间禁止拖拽。 | [跨域 iframe 间禁止拖拽](https://issues.chromium.org/issues/326639460) | 跨域 iframe 间，禁止拖拽。 | 121 | 使用同源 iframe。 | 否 | 是 | 否 | |  |
| 专有网络的 preflight 请求不会带上 CORS accept header。 | [For the preflight request for PNA, the 'Accept' request header has been removed](https://chromiumdash.appspot.com/commit/63c40832ff05ded3d3d1b9d1d81b60739a02e6d6) | 专有网络间（内网）的 preflight 请求不会带上 CORS accept header。 | 123 | 为了避免 header 太长导致截断，内网的 preflight 请求无需做 CORS 检测。 | 否 | 是 | 否 | |  |

## 交互相关：

| 简要描述 | 上游社区公告 | 变更描述 | 上游Chromium变更版本 | 适配建议 | 适配后历史版本需要隔离 | 代码层面问题 | 可以静态扫描 | 示例 | 搜索方式 |
| ---- | ----------  | ---------------- | ---------------- | ------- | ---------------- | -------------- | ---------- | ------ | ------ |
| Mouse 以及 Pointer enter leave事件将不再合并。 | [ Non-composed Mouse and Pointer enter/leave events](https://chromestatus.com/feature/5073526551216128) | 将 mouseenter、mouseleave、pointerenter 和 pointerleave 事件中的 event.composed 属性设置为 "false"，以符合规范并修复互操作性差距。 | 116 | 事件将不做合并，导致事件单独发送，业务需适配。 | 否 | 是 | 否 | | |
| onBeforeUnload 事件的返回值处理变化。 | [ Change beforeunload handler dialog condition](https://chromestatus.com/feature/4968823574233088) | onBeforeUnload 返回值如果为空，不弹出确认对话框。 | 117 | 需要弹框，则返回确定的字符串。 | 否 | 是 | 是 |  | onBeforeUnload |
| 跨域 iframe 移动后用户输入事件相关。 | [Discard Input Events To Recently Moved Cross-Origin Iframes](https://chromestatus.com/feature/5079376387637248) | 为了避免恶意诱导点击的安全措施。 | 121 | 禁止使用诱导点击行为。 | 否 | 是 | 否 |  | |
| ifame 中取消 mousedown 时，mouseup及mouseleave事件不再发送到outer frame，而是会继续发送给 iframe。 | [Interoperable mousedown event cancellation in iframe](https://chromestatus.com/feature/5083240891416576) | 遵从其他浏览器规格。 | 122 | iframe 内的 mouseleave mouseup 事件处理，如果要处理 mousedown 被取消时的场景，需重新设计。 | 否 | 是 | 否 |  | |
| 老的 WebKit Mutation Event 被废弃。 | [Deprecate Mutation Events](https://chromestatus.com/feature/5083947249172480) | 性能低，不建议使用。 | 127 | 使用 Mutation Observer API。 | 否 | 是 | 是 | [相关示例参考](#不再支持mutation-events) | DOMSubtreeModified<br />DOMNodeInserted<br />DOMNodeRemoved<br />DOMNodeRemovedFromDocument<br />DOMNodeInsertedIntoDocument<br />DOMCharacterDataModified |
| PointerEvent.getCoalescedEvents 不允许在非安全环境使用。 | [Remove PointerEvent.getCoalescedEvents() from insecure contexts](https://chromestatus.com/feature/4941651093749760) | 确保用户输入内容的安全。 | 129 | 改用安全协议。 | 否 | 是 | 是 | | getCoalescedEvents |
| input 标签如果为 disabled 状态，点击事件不会传递。【未跟随】 | [Click event on disabled input does not bubble to ancestor elements](https://issues.chromium.org/issues/40070983) | input 标签如果为 disabled 状态，点击事件不会传递。 | 116 | 在 input disabled 标签增加 pointer-events:none 设置。 | 否 | 是 | 否 | |  |
| 非 active document 弹出窗口/对话框将引发异常。 | [Web-Facing Change PSA: Throw exception for popovers/dialogs in non-active documents](https://groups.google.com/a/chromium.org/g/blink-dev/c/jRFiIIkXv_k/m/jnPTfg8WBgAJ) | 之前为无反应，现在将返回异常。 | 132 | 做保护，避免异常未捕获导致异常。 | 否 | 是 | 是 | | showModal |

## 多媒体相关：

| 简要描述 | 上游社区公告 | 变更描述 | 上游Chromium变更版本 | 适配建议 | 适配后历史版本需要隔离 | 代码层面问题 | 可以静态扫描 | 示例 | 搜索方式 |
| ---- | ----------  | ---------------- | ---------------- | ------- | ---------------- | -------------- | ---------- | ------ | ------ |
| WebRTC getStats 的 callback 方式废弃，改用 Promise 方式。 | [deprecated：[WebRTC] Unship callback-based legacy getStats()](https://chromestatus.com/feature/4631626228695040) | RTCPeerConnection是WebRTC标准中的一个接口，用于在浏览器之间建立实时的音频、视频和数据通信。getStats() 方法则是 RTCPeerConnection 提供的一个重要工具，用于获取与连接相关的统计信息，帮助开发者监控和调试 WebRTC 会话。RTCPeerConnection 有两个版本的 getStats() 方法。一个是符合规范的版本，它通过解析 Promise 来返回统计报告；另一个是非标准版本，它通过将回调函数作为第一个参数来返回一种截然不同的报告。基于回调函数的版本在 Chrome 浏览器的 117 版本中被移除，但在 121 版本之前可以通过弃用试用（Deprecation Trial）功能继续使用。从 122 版本开始，即使使用弃用试用功能，该 API 也不再可用。 | 122 | 使用新的 promize 方式。 | 否 | 是 | 是 | | getStats |
| WebKit 开头的 fullscreen API 被废弃。 | [Remove Prefixed HTMLVideoElement Fullscreen APIs](https://chromestatus.com/feature/5111638103687168) | 自大约 Chrome 38 版本起，带有前缀的、特定于 HTMLVideoElement 的全屏应用程序编程接口（API）就已被弃用。它们已被 Element.requestFullscreen() API 所取代，该无前缀的标准 API 于 2018 年的 Chrome 71 版本首次发布。截至 2024 年，大多数浏览器已经支持无前缀的 API 数年之久。此功能旨在从 HTMLVideoElement 中移除以下 API：<br />只读属性 boolean webkitSupportsFullscreen;<br />只读属性 boolean webkitDisplayingFullscreen;<br />方法 void webkitEnterFullscreen();<br />方法 void webkitExitFullscreen();<br />// 注意 FullScreen 中字母 “S” 的大小写差异<br />方法 void webkitEnterFullScreen();<br />方法 void webkitExitFullScreen();<br />这些方法如今仅仅是现代 API 的别名。多年来，它们的使用量持续下降。 | 132 | 改用 requestFullscreen 标准的 w3c api。 | 否 | 是 | 是 | [相关示例参考](#删除了前缀的视频fullscreen-api) | webkitSupportsFullscreen<br />webkitDisplayingFullscreen<br />webkitEnterFullscreen<br />webkitExitFullscreen<br />webkitEnterFullScreen<br />webkitExitFullScreen |

## 网络相关：

| 简要描述 | 上游社区公告 | 变更描述 | 上游Chromium变更版本 | 适配建议 | 适配后历史版本需要隔离 | 代码层面问题 | 可以静态扫描 | 示例 | 搜索方式 |
| ---- | ----------  | ---------------- | ---------------- | ------- | ---------------- | -------------- | ---------- | ------ | ------ |
| ORB 更严格。 | [Opaque Response Blocking(ORB, aka CORB++) v0.2](https://chromestatus.com/feature/5166834424217600) | ORB用于阻止跨域资源请求。 | 117 | 跨域请求进行改进。 | 是（api 20隔离） | 否 | 否 | |  |
| URL 中允许在path中存在 %00， 但 host 中仍不允许。 | [URL: Allow "%00" as a valid URL path](https://chromestatus.com/feature/4763113357836288) | 允许URL path中包含 %00 字符。 | 117 | 用户输入的url检查需注意。 | 否 | 否 | 否 | |  |
| cookie 中包含控制字符将废弃。 | [Block all cookies set via JavaScript that contain control characters](https://chromestatus.com/feature/5709264560586752) | 如果JS设置的cookie中包含控制字符（control character，比如回撤，换行，null等），以前是截断，现在改为丢弃整个cookie。 | 118 | 不要包含控制字符。 | 否 | 否 | 否 | |  |
| URL host 中包含特殊字符的处理规则变更。 | [Standard compliant URL host punctuation characters ](https://chromestatus.com/feature/5074885224693760) | 符合 W3C URL 规范。 | 119 | host包含括号等特殊字符，new Url会触发 exception，要做异常捕获。 | 否 | 否 | 否 | |  |
| URL path 规格变更。 | [Make URL parser to not decode percent-encoded ASCII characters in URL path](https://chromestatus.com/feature/6389236793606144) | 解析url时，path部分的百分号字符（比如%41）不解码。 | 118 | 之前强依赖转译的，需要检查 132 内核。 | 是 | 否 | 否 | |  |
| 为了安全，targetName 中包含特殊字符，则变为空白页面。 | [Replace dangling markup in target name to \`\_blank\`](https://chromestatus.com/feature/5073969773805568) | 遵守URL规范，host部分某些字符不做兼容处理，直接报错。 | 119 | targetName 中不能包含特殊字符。 | 否 | 否 | 否 | |  |
| 自动切到 http3，如果服务端支持。 | [Feature: HTTP/3 protocol upgrade for HTTPS DNS records with h3 alpn parameter](https://chromestatus.com/feature/5154357283651584) | UseDnsHttpsSvcbAlpn 默认开启，预链接HTTP 改为预链接QUIC 导致XTS超时失败。 | 118 | 检查DNS与服务端 http3 协议是否配置一致。 | 否 | 否 | 否 | |  |
| 性能监控：PerformanceResourceTiming 的 initiatorType 发生变更。 | [Use "script" as the initiatorType in resource timing for modules](https://chromium-review.googlesource.com/c/chromium/src/+/5424715) | worker 加载 url 时，用 PerformanceResourceTiming 做性能监控 initiatorType 类型从 other 变为 script。 | 132 | 无需太多关注，性能监控相关。 | 否 | 否 | 是 | | initiatorType == "other" |

## W3C 标准相关：

| 简要描述 | 上游社区公告 | 变更描述 | 上游Chromium变更版本 | 适配建议 | 适配后历史版本需要隔离 | 代码层面问题 | 可以静态扫描 | 示例 | 搜索方式 |
| ---- | ----------  | ---------------- | ---------------- | ------- | ---------------- | -------------- | ----------  | ------ | ------ |
| 将 XML 文档中的相邻 CDATA 节点合并为一个 CDATA 节点。 | [XML documents merge consecutive CDATA sections into single node](https://chromestatus.com/feature/5168429241204736) | 将 XML 文档中的相邻 CDATA 节点合并为一个 CDATA 节点。 | 118 | 排查使用libxml传递超过300字节CDATA数据场景，避免超过300字节。 | 否 | 否 | 否 |  |  |
| 去除了对WebSQL的支持。 | [Deprecate and Remove WebSQL](https://chromestatus.com/feature/5134293578285056) | 去除了对WebSQL的支持，应用可使用W3C标准的IndexDB。 | 119 | 替换为 IndexDB 实现。 | 否 | 是 | 是 | | openDatabase<br />executeSql |
| 废弃非标属性shadowroot。 | [Deprecate non-standard \`shadowroot\` attribute for declarative shadow DOM](https://chromestatus.com/feature/6239658726391808) | 旧的、非标准的`shadowroot`属性现在已弃用，有一个简单的迁移路径：将`shadowroot`替换为`shadowrootmode`。 | 119 | 将`shadowroot`替换为`shadowrootmode`。 | 否 | 是 | 是 | | shadowroot |
| 废弃非标准的shoadowroot getInnerHTML方法。 | [Feature: Declarative shadow DOM serialization](https://chromestatus.com/feature/5102952270528512) | 废弃非标准化的 `shadowroot getInnerHTML`接口，W3C 2020 年的草案，2023 年废弃。 | 125 | 将getInnerHTML替换为getHtml。 | 否 | 是 | 是 | | getInnerHTML |
| 废弃Sanitizer API。 | [Remove Sanitizer API](https://chromestatus.com/feature/5115076981293056) | 下线M109上线的sanitizer api，重新设计和实现。API提供给web开发者用来为了安全性禁止某些脚本的执行。 | 119 | 判断版本，新版本使用旧版 `Sanitizer` 的代码需迁移至新方法（如 `element.setHTML(html, {sanitizer: config})`）。 | 否 | 是 | 是 | | Sanitizer |
| Transitions API不允许传入空参数。 | [View Transitions: making callback non-nullable](https://chromestatus.com/feature/5194510942601216) | startViewTransition 调用当前接受一个可选的可为空回调类型，默认值为 null，将其更改为不可为空类型。 | 120 | startViewTransition接口传入非空回调类型。 | 否 | 是 | 是 | | startViewTransition不能为空参数 |
| 当查询条件中包含浏览器不支持的特性时，整个条件将直接视为不匹配。 | [CSS @container with unsupported Features never match](https://chromestatus.com/feature/5105423604776960) | SS `@container` 是容器查询（Container Queries）的核心语法，允许开发者根据父容器的尺寸或样式动态调整内部元素的样式。Chromium 近期的一项变更调整了其处理 `@container` 条件的逻辑：当查询条件中包含浏览器不支持的特性时，整个条件将直接视为不匹配，而非忽略不支持的部分。这可能导致某些预期生效的样式失效。 | 122 | 将包含不支持特性的条件拆分为独立的 `@container` 规则，避免因单个特性导致整体失效。 | 否 | 是 | 是 | | @container |
| backdrop继承逻辑改变。 | [CSS Pseudo Element ::backdrop inheriting from Originating Element](https://chromestatus.com/feature/4875749691752448) | ::backdrop伪元素用于从初始值继承。这意味着除非直接在::backdrop规则上指定，否则::backdrop不能使用自定义属性值。规范现在已更改，::backdrop从原元素继承，并且实现了这一点。 | 122 | 114上必须在backdrop中显式指定，132上可以会继承父类元素，backdrop css 属性行为遵循规范。 | 是 | 是 | 是 | | ::backdrop |
| inline script 也可以阻塞渲染，加上 blocking 标签，之前不阻塞渲染。 | [ blocking=render on inline module scripts](https://chromestatus.com/feature/5200457540829184) | 改动解除老接口限制，可能会触发意外的暂停渲染，原来script标签必须要有src属性，才会暂停渲染。 | 123 | <script blocking="render"type="module">会阻塞渲染，无需src属性。 | 如果必须需要阻塞渲染，建议不要 inline | 是 | 是 | | blocking="render" |
| 删除亮度接近 100% 或 0 的 （ok）lab/lch 颜色的不连续性。 | [Remove Discontinuity for (ok)lab/lch colors with lightness of nearly 100% or 0](https://chromestatus.com/feature/5534009582157824) | 删除亮度接近 100% 或 0 的 （ok）lab/lch 颜色的不连续性。 | 125 | 颜色渐变设置生效，不会显示纯黑或者纯白。 | 否 | 是 | 是 | |  |
| navigator.permissions.query({name: 'window-placement'})  这种用法将不支持。 | [Remove window-placement alias for permission and permission policy descriptors](https://chromestatus.com/feature/5137018030391296) | 遵循 W3C 规范。 | 125 | window-placement 换成 window-management。 | 否 | 是 | 是 | | window-placement |
| 让CSSPageRule继承于CSSGroupingRule而不是CSSRule。 | [CSSPageRule to inherit from CSSGroupingRule](https://chromestatus.com/feature/5218866421825536) | 让 CSSPageRule 继承自 CSSGroupingRule 而不是 CSSRule。 | 126 | 继承关系改变，检查CSSGroupingRule的类型判断逻辑。 | 是 | 是 | 是 | |  |
| position-try-options 重命名为 position-try-fallbacks。 | [Rename position-try-options to position-try-fallbacks](https://chromestatus.com/feature/5090673808900096) | 将 position-try-options 重命名为 position-try-fallbacks。 | 128 | position-try-options 替换为 position-try-fallbacks。 | 是 | 是 | 是 | | position-try-options |
| 嵌套css样式的写法，按照先后顺序，而非嵌套顺序。 | [CSS Nesting: The Nested Declarations Rule](https://chromestatus.com/feature/5084403030818816) | 嵌套css样式的写法，按照先后顺序，而非嵌套顺序。 | 130 | 嵌套css样式的写法，按照先后顺序生效，而非嵌套顺序。 | 是 | 是 | 是 | |  |
| 修复Selection.isCollapsed()在 shadowDOM shu树中一致返回 true 的问题。 | [Fix Selection isCollapsed in Shadow DOM](https://chromestatus.com/feature/5175599392620544) | 114上选择的锚点在阴影树中，即使本身没有折叠，也会返回true。132上已修复。 | 132 | 遵循标准。 | 是 | 是 | 是 | | isCollapsed |
| 弃用 assert 关键字，并用新的 with 关键字替代它在模块导入语法中的用法。 | [Deprecate and remove import assertion 'assert' syntax - Chrome Platform Status](https://chromestatus.com/feature/4689167795879936) | 废弃导入模块时的assert语法，例如`import m from 'foo' assert { type: 'json' }` 会报错SyntaxError，开发者需要将其改为 `import m from 'foo' with { type: 'json' }`。 | 126 |弃用 assert 关键字，并用新的 with 关键字替代它在模块导入语法中的用法。 | 否 | 是 | 是 | |assert |
| v8 废弃 Array group 以及 groupToMap 语法。 | [Array.prototype.group系列相关方法改为静态方法Object.group](https://github.com/tc39/proposal-array-grouping) | Array.prototype.group以及Array.prototype.groupToMap不再使用，改为用静态方法Object.group。 | 117 | v8 废弃 Array group 以及 groupToMap 语法。 | 否 | 是 | 是 | | group<br />groupToMap |
| AsyncFromSyncIteratorPrototype 行为规范化。 | [AsyncFromSyncIteratorPrototype.throw方法行为规范化](https://github.com/tc39/ecma262/pull/2600) |  当Async-from-sync.throw()未定义时，抛出的异常信息从undefined变更为无限制。 | 126 | 遵循标准。 | 是 | 是 | 否 | |  |
| Temporal.Duration范围检查的加强与规范化。 | [Temporal.Duration范围检查的加强与规范化](https://github.com/tc39/ecma262/pull/3092) | Temporal.Duration增加检查，在绝对值超出2^32时抛出RangeError。 |  | 遵循标准。 | 否 | 是 | 是 | |  |
| Symbol.toStringTag 返回值类型变化。 | [Iterator.prototype的Symbol.toStringTag属性由Object Object变更为Object Iterator](https://github.com/tc39/proposal-iterator-helpers/pull/287) | Iterator.prototype的Symbol.toStringTag属性由Object Object变更为Object Iterator。 | 117 | 遵循标准。 | 是 | 是 | 否 | |  |
| JS 参数个数限制为 65535，之前无限制。 | 函数参数数量增加限制。 | v8规格变更，114版本无数量限制，132版本限制函数参数为65535。 | 128 | 遵循标准。 | 否 | 是 | 是 | |  |
| wasm 全局对象默认值变更。 | wasm 全局对象默认值变更。 | wasm externref 类型全局对象默认值由null变为undefine。 | 116 | 遵循标准。 | 否 | 是 | 否 | |  |
| 日期、时区等日志格式。 | 日期、时区等日志格式。 | 遵循CLDR等外部规范时区映射或日期格式变化，如：<br />DateTimeFormat、Intl.Locale：Intl.DateTimeFormat("en", {hour:"numeric", hour12:false})的值从h24变更为h23;<br />timezone：Intl.getCanonicalLocales("und-u-tz-est")的结果从und-u-tz-utcw05变更为und-u-tz-party；<br />日志打印格式变化，如JSON.parse异常新增行列号信息、字符串加双引号""等。 |  | 遵循标准。 | 否 | 是 | 否 | |  |
| V8引擎部分bugfix修复，导致部分行为变更。 | V8引擎部分bugfix修复。 | [Error.stack](https://chromium-review.googlesource.com/c/v8/v8/+/4459251)、BigInt长度限制、[Regexp字符串匹配](https://issues.chromium.org/issues/42203320)、new Class行为规范化、[Float64Array.from.call](https://issues.chromium.org/issues/336841825)行为规范化等。 |  | 遵循标准。 | 否 | 是 | 否 | |  |
| CSS Grid 类型与 aspect-ratio 共用，有极端场景的layout问题。 | https://issues.chromium.org/issues/340363672<br />https://issues.chromium.org/issues/338601202<br />https://issues.chromium.org/issues/337356072 | 上游社区优化 grid 布局引入。 |  | 规避这种特殊用法。 | 否 | 否 | 否 | |  |
| 当伪元素参数以单个冒号开头且无法解析时，正确的结果是一个空的 CSSStyleDeclaration，而不是元素的 CSSStyleDeclaration。 | 上游issue见：https://bugs.webkit.org/show_bug.cgi?id=266986<br />提交见：https://github.com/web-platform-tests/wpt/pull/44119 | 上游社区修改 pseudo-element元素传入单个冒号开头的参数时，无法被正确解析的场景，将会返回空。 |  | 遵循标准。 | 是 | 是 | 是 | | pseudo-element argument starts with a single colon、empty CSSStyleDeclaration. |
| 元素同时设置content-intrinsic width 和 content-visibility: hidden时获取尺寸失败。 | chromium上游bug尚未修复，见：https://issues.chromium.org/issues/327723279 | chromium上游bug尚未修复。 |  | 遵循标准。 | 是 | 是 | 是 | | content-intrinsic width、content-visibility: hidden |
| 将text-wrap拆分为两个属性text-wrap-mode & text-wrap-style。 | 社区上游变更，见：https://bugs.webkit.org/show_bug.cgi?id=263432 | 将text-wrap拆分为两个属性text-wrap-mode & text-wrap-style。 |  | 遵循标准。 | 是 | 是 | 是 | | text-wrap、text-wrap-mode & text-wrap-style |
| 对于pseudo元素，匹配时必须是双冒号开头。 | 社区上游变更，见：<br />https://bugs.webkit.org/show_bug.cgi?id=266862<br />https://github.com/WebKit/WebKit/pull/22231 | 对于pseudo元素，匹配时必须是双冒号开头，原有单冒号写法会报错。 |  | 遵循标准。 | 否 | 是 | 是 | | PseudoElement |
| 文字方向不会通过shadowdom继承。 | 社区上游变更，见：<br />[Issue #3699 · whatwg/html](https://github.com/w3c/csswg-drafts/issues/4805) | 文字方向不会通过shadowdom继承。 |  | 遵循标准。 | 是 | 是 | 是 | | inherited into Shadow DOM |
| ::details-content设置让details元素的display属性一直为display:block。 | 社区上游变更，见：https://issues.chromium.org/issues/40068507 | 这（前提是启用了 DetailsStyling）使得::details-content 始终显示为display:block，而不仅仅是在details关闭时。 |  | 遵循标准。 | 是 | 是 | 是 | | details、::details-content、display:block |
| 将伪类语法从--修改成state()语法。 | https://chromestatus.com/feature/5140610730426368 | 伪类定义:--语法变更为:state()。 |  | 遵循标准。 | 是 | 是 | 是 | | switch :--foo to :state(foo) |
| mo元素的minsize和maxsize修改为基于拓展前的size做比例变化。 | 社区上游变更，见：[Issue #103 · w3c/mathml-core](https://github.com/w3c/mathml-core/issues/103)，<br />社区提交见：https://chromium-review.googlesource.com/c/chromium/src/+/5402478 | 1、mo元素百分比的minsize和maxsize值，按照w3c标准修改成相对于unstretched size的百分比，影响MathML元素的尺寸。<br />2、在虚拟像素比无法被前端设置的大小整除的设备上，尺寸计算会有一定的误差。 |  | 遵循标准。 | 是 | 是 | 是 | | MathML、mo element、minsize/maxszie |
| 对于不存在的字体，document.fonts.check接口将返回true，而不是false。 | https://github.com/w3c/csswg-drafts/pull/5757 | 移除FontFaceSet.check()对不存在字体返回false的逻辑。 |  | 遵循标准。使用接口替换：https://developer.mozilla.org/en-US/docs/Web/API/FontFaceSet/loadingerror_event | 否 | 是 | 是 | [相关示例参考](#fontfacesetcheck对于不存在的字体返回false的逻辑发生变化) | document.fonts.check |
| window.screen.availWidth/availHeight将返回屏幕宽高而不是Web组件宽高。 | 平台适配变更 | window.availWidth/availHeight将返回屏幕宽高而不是Web组件宽高。 |  | 遵循标准。 | 是 | 是 | 是 | | window.screen.availWidth/availHeight |
| DataTransfer 的 clearData 行为变更。 | https://issues.chromium.org/issues/40884581 | 之前会清理 file 新协议的内容，新版本不删除。 | 122 | 修正早期的标准实现错误 | 是 | 是 | 是 | [相关示例参考](#datatransfercleardata-错误删除携带文件) | clearData |
| CSS zoom 遵从标准实现 | https://chromestatus.com/feature/5198254868529152 | 更新现有的非标准 CSS zoom 属性的实现，以符合新标准。这改变了各种 JS API 以符合规范（参见设计文档），将 zoom 应用于 iframe 内容文档，并将其应用于所有继承的长度属性（以前它只改变继承的字体大小）| 128 | 遵循标准。 | 否 | 否 | 否 |  | 使用了 zoom, getBoundingClientRect, getClientRects 等 |
| justify-content:space-around 样式变更 | https://chromium-review.googlesource.com/c/chromium/src/+/5250231 | justify-content:space-around 样式在子组件宽度总和大于父组件时行为发生变更，不再是从中间向左右溢出，而是向右溢出。| 123 | 遵循标准。 | 否 | 是 | 是 |  | justify-content:space-around |


## 相关示例：

### FontFaceSet.check()对于不存在的字体返回false的逻辑发生变化

114版本在FontFaceSet.check()如果检测到了不存在的字体，会返回false。所以很多人会用这个方法来检测字体到底有没有引入。

132对此进行了修改，如果目标字体没有引入，会仍然返回true。

下列类似代码可能会产生行为变更。

```typescript
const fontSpec = `${fontSize} ${fontFamily}`;
const isLoaded = document.fonts.check(fontSpec);
if (isLoaded) {
  console.log(`字体 ${fontFamily} 已加载成功`);
  return true;
} else {
  console.log(`字体 ${fontFamily} 未加载成功`);
  return false;
}
```

### DataTransfer.clearData() 错误删除携带文件

从接口语义以及W3C标准来看，DataTransfer的clearData接口只应该清除通过setData设置而来的格式数据，不应该清除文件。<br>
W3C标准也清晰注明：<br>The clearData() method does not affect whether any files were included in the drag, so the types attribute's list might still not be empty after calling clearData() (it would still contain the "Files" string if any files were included in the drag).

参考https://html.spec.whatwg.org/multipage/dnd.html#dom-datatransfer-cleardata-dev

```typescript
// 1. 创建一个dataTransfer对象。
const dataTransfer = new DataTransfer();

// 2. 给这个dataTransfer对象添加一个图片。
const file = new File(["1"], "1.png", {
  type: "image/png"
});
dataTransfer.items.add(file);

// 3. 调用clearData接口。
dataTransfer.clearData();

// 114此处会发现删除了文件，132修复了该问题，不再会错误删除。
console.log("items after", dataTransfer.items.length, dataTransfer.types);
```

### 移除了对Theora和vp8的支持
Theora的视频是一种目前使用率较低的一种视频格式，并且目前也有一定的安全风险。

```html
<div class="video-container">
  <video controls width="640" height="360" preload="metadata" playsinline webkit-playsinline>
    <source src="./flower.ogv" type="video/ogg">
    您的浏览器不支持视频播放。
    </video>
</div>
```

type="video/ogg"的video，114能够播放，132不再能够播放。

### 删除了带前缀的视频Fullscreen API
4.  删除了带前缀的视频Fullscreen API
带前缀的Fullscreen API有如下几种
只读属性 boolean webkitSupportsFullscreen;
只读属性 boolean webkitDisplayingFullscreen;
方法 void webkitEnterFullscreen();
方法 void webkitExitFullscreen();
// 注意 FullScreen 中字母 “S” 的大小写差异
方法 void webkitEnterFullScreen();
方法 void webkitExitFullScreen();

建议使用不带前缀的全屏API。

```html
<script>
  function enterFullScreen() {
    const video = document.getElementById('testVideo');
    let isSupportFullscreen = video.webkitSupportsFullscreen; // 114有效，132无效。
    let isFullscreen = video.webkitDisplayingFullscreen; // 114有效，132无效。
    video.webkitEnterFullscreen(); // 114有效，132无效。
    video.webkitEnterFullScreen(); // 114有效，132无效。
  }

  function exitFullScreen() {
    const video = document.getElementById('testVideo');
    video.webkitExitFullscreen(); // 114有效，132无效。
    video.webkitExitFullScreen(); // 114有效，132无效。
  }
</script>

<button onclick="enterFullScreen()">进入全屏</button>
<button onclick="exitFullScreen()">退出全屏</button>
<video id="testVideo" controls>
  <source src="flower.mp4" type="video/mp4">
  您的浏览器不支持视频播放。
</video>
```

### 不再支持Mutation Events

Mutation Events因为性能问题被弃用，可以被更高效的Mutation Observer API所取代。建议都使用Mutation Observer 来完成替代功能。

由于MutationObserver的事件类型和使用方式与Mutation Events略有区别，建议根据具体需求来进行判断。

基本事件类型大致映射关系如下表格所示：

| 旧的 Mutation Event 类型 | 对应的 MutationRecord.type | MutationObserver 配置选项 | 说明 |
| ------------------------ | -------------------------- | -------------------------- | ---- |
| DOMNodeInserted | childList | `{ childList: true }` | 当 `mutation.addedNodes` 列表不为空时，相当于 DOMNodeInserted。 |
| DOMNodeRemoved | childList | `{ childList: true }` | 当 `mutation.removedNodes` 列表不为空时，相当于 DOMNodeRemoved。 |
| DOMNodeInsertedIntoDocument | childList | `{ childList: true, subtree: true }` | 监听整个文档的 `childList` 变化。当 `mutation.addedNodes` 不为空时，遍历这些节点，如果节点的 `isConnected` 属性为 `true`，则表示该节点已被插入文档。 |
| DOMNodeRemovedFromDocument | childList | `{ childList: true, subtree: true }` | 监听整个文档的 `childList` 变化。当 `mutation.removedNodes` 不为空时，这个列表中的所有节点都刚刚从文档中被移除。 |
| DOMAttrModified | attributes | `{ attributes: true }` | 监听属性变更。`mutation.attributeName` 提供被修改的属性名。 |
| DOMCharacterDataModified | characterData | `{ characterData: true }` | 监听文本节点或注释节点内容的变更。 |
| DOMSubtreeModified | childList, attributes, characterData | `{ subtree: true }`（并结合需要的其他选项，如 `childList: true`, `attributes: true`, `characterData: true`） | 通过将 `subtree` 设为 `true`，并联合相关类型的配置，可以监听整个子树下的所有变更，达到类似效果。 |

示例代码：

```html
<script>
  const mutationEvents = [
    'DOMSubtreeModified',
    'DOMNodeInserted', 
    'DOMNodeRemoved',
    'DOMNodeInsertedIntoDocument',
    'DOMNodeRemovedFromDocument',
    'DOMCharacterDataModified'
  ];

  function handleMutationEvent(event) {
    // 114会回调，132不会回调
  }
  mutationEvents.forEach(eventType => {
    targetContainer.addEventListener(eventType, handleMutationEvent, true);
    document.addEventListener(eventType, handleMutationEvent, true);
  });
  
  // 推荐使用MutationObserver，下面是等价代码:
  // 在页面加载完毕时或者合适的时候主动添加监听
  document.addEventListener('DOMContentLoaded', function() {
      const targetContainer = document.getElementById('targetContainer');
      const textInput = document.getElementById('textInput');

      let observer = null;

      function startMutationEvents() {
          observer = new MutationObserver(function(mutations) {
              mutations.forEach(function(mutation) {
                  // 针对不同类型节点变动事件进行处理
                  switch (mutation.type) {
                      case 'childList':
                          mutation.addedNodes.forEach(function(node) {
                              console.log('[MutationObserver] DOMNodeInserted ->', node);
                          });
                          mutation.removedNodes.forEach(function(node) {
                              console.log('[MutationObserver] DOMNodeRemoved ->', node);
                          });
                          break;
                      case 'attributes':
                          console.log('[MutationObserver] Attributes modified ->', mutation.target, 'attr:', mutation.attributeName);
                          break;
                      case 'characterData':
                          console.log('[MutationObserver] DOMCharacterDataModified ->', mutation.target);
                          break;
                  }
              });
          });

          // 可以根据需要有选择的监听
          observer.observe(targetContainer, {
              childList: true,
              attributes: true,
              characterData: true,
              subtree: true
          });
          console.log('MutationObserver 监听已启动');
      }

      startMutationEvents();
  });

  function addElement() {
    const text = textInput.value || '新元素';
    const newElement = document.createElement('div');
    newElement.className = 'demo-item';
    newElement.textContent = text + ' - ' + Date.now();
    newElement.dataset.id = Date.now();
    
    targetContainer.appendChild(newElement);
  }
</script>

<div class="controls">
    <button onclick="startMutationEvents()" class="success">启动 Mutation Events 监听</button>
</div>

<div class="input-group">
    <input type="text" id="textInput" placeholder="输入要添加的文本内容" value="新元素">
    <button onclick="addElement()">添加元素</button>
</div>

<div id="targetContainer">
    <div class="demo-item" data-id="1">初始元素 1</div>
    <div class="demo-item" data-id="2">初始元素 2</div>
    <div class="demo-item" data-id="3">初始元素 3</div>
</div>
```