diff --git a/core/fxge/BUILD.gn b/core/fxge/BUILD.gn
index 9b891cb..a3ad26a 100644
--- a/core/fxge/BUILD.gn
+++ b/core/fxge/BUILD.gn
@@ -163,7 +163,7 @@ source_set("fxge") {
     ]
   }
 
-  if (is_android) {
+  if (is_android || is_ohos) {
     sources += [
       "android/cfpf_skiafont.cpp",
       "android/cfpf_skiafont.h",
diff --git a/core/fxge/cfx_face.cpp b/core/fxge/cfx_face.cpp
index 04f5d8a..c79c2e5 100644
--- a/core/fxge/cfx_face.cpp
+++ b/core/fxge/cfx_face.cpp
@@ -616,7 +616,7 @@ std::optional<std::array<uint32_t, 4>> CFX_Face::GetOs2UnicodeRange() {
 }
 #endif  // defined(PDF_ENABLE_XFA)
 
-#if defined(PDF_ENABLE_XFA) || BUILDFLAG(IS_ANDROID)
+#if defined(PDF_ENABLE_XFA) || BUILDFLAG(IS_ANDROID) || BUILDFLAG(IS_OHOS)
 std::optional<std::array<uint32_t, 2>> CFX_Face::GetOs2CodePageRange() {
   auto* os2 = static_cast<TT_OS2*>(FT_Get_Sfnt_Table(GetRec(), FT_SFNT_OS2));
   std::optional<std::array<uint32_t, 2>> ft_result;
@@ -672,7 +672,8 @@ std::optional<std::array<uint8_t, 2>> CFX_Face::GetOs2Panose() {
 
   return ft_result;
 }
-#endif  // defined(PDF_ENABLE_XFA) || BUILDFLAG(IS_ANDROID)
+#endif  // defined(PDF_ENABLE_XFA) || BUILDFLAG(IS_ANDROID) ||
+        // BUILDFLAG(IS_OHOS)
 
 int CFX_Face::GetGlyphCount() const {
   const int ft_result = pdfium::checked_cast<int>(GetRec()->num_glyphs);
@@ -1440,7 +1441,7 @@ void CFX_Face::AdjustVariationParams(int glyph_index,
   FT_Set_MM_Design_Coordinates(rec, 2, coords);
 }
 
-#if defined(PDF_ENABLE_XFA) || BUILDFLAG(IS_ANDROID)
+#if defined(PDF_ENABLE_XFA) || BUILDFLAG(IS_ANDROID) || BUILDFLAG(IS_OHOS)
 uint32_t CFX_Face::GetFontStyle() {
   uint32_t style = 0;
   if (IsBold()) {
@@ -1468,4 +1469,5 @@ uint32_t CFX_Face::GetFontStyle() {
   }
   return style;
 }
-#endif  // defined(PDF_ENABLE_XFA) || BUILDFLAG(IS_ANDROID)
+#endif  // defined(PDF_ENABLE_XFA) || BUILDFLAG(IS_ANDROID) ||
+        // BUILDFLAG(IS_OHOS)
diff --git a/core/fxge/cfx_face.h b/core/fxge/cfx_face.h
index 759e2e0..32675b7 100644
--- a/core/fxge/cfx_face.h
+++ b/core/fxge/cfx_face.h
@@ -138,7 +138,7 @@ class CFX_Face final : public Retainable, public Observable {
   void SetCharMapByIndex(size_t index);
   bool SelectCharMap(fxge::FontEncoding encoding);
 
-#if defined(PDF_ENABLE_XFA) || BUILDFLAG(IS_ANDROID)
+#if defined(PDF_ENABLE_XFA) || BUILDFLAG(IS_ANDROID) || BUILDFLAG(IS_OHOS)
   // Returns enum FontStyle values.
   uint32_t GetFontStyle();
 
@@ -183,7 +183,7 @@ class CFX_Face final : public Retainable, public Observable {
 
   pdfium::span<const FT_CharMap> GetCharMaps() const;
 
-#if BUILDFLAG(IS_ANDROID) || defined(PDF_ENABLE_XFA)
+#if BUILDFLAG(IS_ANDROID) || defined(PDF_ENABLE_XFA) || BUILDFLAG(IS_OHOS)
   std::optional<std::array<uint8_t, 2>> GetOs2Panose();
 #endif
 
