diff --git a/BUILD.gn b/BUILD.gn
index a19ec521..0d886394 100644
--- a/BUILD.gn
+++ b/BUILD.gn
@@ -1131,6 +1131,11 @@ if (target_os == "android") {
 } else if (target_os == "chromeos") {
   enabled_external_v8_defines += [ "V8_HAVE_TARGET_OS" ]
   enabled_external_v8_defines += [ "V8_TARGET_OS_CHROMEOS" ]
+} else if (target_os == "ohos") {
+  # HarmonyOS is a musl-based Linux target. Without an explicit target OS,
+  # v8config.h defines an empty V8_TARGET_OS_LINUX that breaks #if checks.
+  enabled_external_v8_defines += [ "V8_HAVE_TARGET_OS" ]
+  enabled_external_v8_defines += [ "V8_TARGET_OS_LINUX" ]
 }
 
 # Some V8_TARGET_ARCH_ defines that affect Api constants (see usages of
@@ -5077,7 +5082,7 @@ v8_header_set("v8_internal_headers") {
     if (v8_enable_webassembly) {
       # iOS Xcode simulator builds run on an x64 target. iOS and macOS are both
       # based on Darwin and thus POSIX-compliant to a similar degree.
-      if (is_linux || is_chromeos || is_mac || is_ios ||
+      if (is_linux || is_chromeos || is_mac || is_ios || is_ohos ||
           target_os == "freebsd") {
         sources += [ "src/trap-handler/handler-inside-posix.h" ]
       } else if (is_win) {
@@ -5145,7 +5150,7 @@ v8_header_set("v8_internal_headers") {
       # Trap handling is enabled on arm64 Mac and Linux and in simulators on
       # x64 on Linux and Mac.
       if ((current_cpu == "arm64" &&
-           (is_linux || is_chromeos || is_mac || is_ios)) ||
+           (is_linux || is_chromeos || is_mac || is_ios || is_ohos)) ||
           (current_cpu == "x64" && (is_linux || is_chromeos || is_mac))) {
         sources += [ "src/trap-handler/handler-inside-posix.h" ]
       }
@@ -6575,7 +6580,7 @@ v8_cluster_source_set("v8_base_without_compiler") {
     if (v8_enable_webassembly) {
       # iOS Xcode simulator builds run on an x64 target. iOS and macOS are both
       # based on Darwin and thus POSIX-compliant to a similar degree.
-      if (is_linux || is_chromeos || is_mac || is_ios ||
+      if (is_linux || is_chromeos || is_mac || is_ios || is_ohos ||
           target_os == "freebsd") {
         sources += [
           "src/trap-handler/handler-inside-posix.cc",
@@ -6627,7 +6632,8 @@ v8_cluster_source_set("v8_base_without_compiler") {
     ]
     if (v8_enable_webassembly) {
       # Trap handling is enabled on arm64 and x64, on Linux, Mac and Windows.
-      if ((current_cpu == "arm64" && (is_linux || is_chromeos || is_apple)) ||
+      if ((current_cpu == "arm64" &&
+           (is_linux || is_chromeos || is_apple || is_ohos)) ||
           (current_cpu == "x64" && (is_linux || is_chromeos || is_mac))) {
         sources += [
           "src/trap-handler/handler-inside-posix.cc",
@@ -7286,7 +7292,7 @@ v8_component("v8_libbase") {
     }
   }
 
-  if (is_linux || is_chromeos) {
+  if (is_linux || is_chromeos || is_ohos) {
     sources += [
       "src/base/debug/stack_trace_posix.cc",
       "src/base/platform/platform-linux.cc",
diff --git a/gni/v8.gni b/gni/v8.gni
index 6334faf2..bdaa8e4d 100644
--- a/gni/v8.gni
+++ b/gni/v8.gni
@@ -351,6 +351,7 @@ is_drumbrake_supported =
     (v8_current_cpu == "x64" || v8_current_cpu == "arm64" ||
      v8_current_cpu == "riscv64") &&
     (target_os == "win" || target_os == "linux" || target_os == "mac" ||
+     target_os == "ohos" ||
      target_os == "ios")
 
 # Turbofan is enabled by default, except in lite mode.
diff --git a/src/base/debug/stack_trace_posix.cc b/src/base/debug/stack_trace_posix.cc
index 39fa8be2..ca2eb32c 100644
--- a/src/base/debug/stack_trace_posix.cc
+++ b/src/base/debug/stack_trace_posix.cc
@@ -64,6 +64,7 @@ namespace {
 volatile sig_atomic_t in_signal_handler = 0;
 bool dump_stack_in_signal_handler = true;
 
+#if HAVE_EXECINFO_H
 // The prefix used for mangled symbols, per the Itanium C++ ABI:
 // http://www.codesourcery.com/cxx-abi/abi.html#mangling
 const char kMangledSymbolPrefix[] = "_Z";
@@ -73,7 +74,6 @@ const char kMangledSymbolPrefix[] = "_Z";
 const char kSymbolCharacters[] =
     "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789_";
 
-#if HAVE_EXECINFO_H
 // Demangles C++ symbols in the given text. Example:
 //
 // "out/Debug/base_unittests(_ZN10StackTraceC1Ev+0x20) [0x817778c]"
diff --git a/src/builtins/arm64/builtins-arm64.cc b/src/builtins/arm64/builtins-arm64.cc
index 6a7717c9..2aa28a1b 100644
--- a/src/builtins/arm64/builtins-arm64.cc
+++ b/src/builtins/arm64/builtins-arm64.cc
@@ -402,7 +402,10 @@ static void GetSharedFunctionInfoBytecodeOrBaseline(
     Label* is_baseline, Label* is_unavailable) {
   ASM_CODE_COMMENT(masm);
   DCHECK(!AreAliased(bytecode, scratch1));
-  Label is_interpreter_data, is_bytecode_array, is_code;
+  Label is_interpreter_data, is_bytecode_array;
+#if !V8_JITLESS_BOOL
+  Label is_code;
+#endif
 
   Register data = bytecode;
   __ LoadTrustedUnknownPointerField(
